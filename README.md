# Pickleball Signup v3.1.1 — Site Editor Fix

Built from v3.1 Visual Page Builder.

## Changes
- Branding, Page Builder, and Labels are now real clickable tabs.
- Restored editable Waiting title and Waiting message.
- Added editable minimum player count before booking.
- Use `{min}` in the waiting message to automatically display the configured minimum.
- The readiness badge uses the same configured minimum.
- Live Desktop/Mobile preview updates before Publish.
- Firebase Authentication, Firestore events, signups, notifications, usernames, and site settings remain connected.
